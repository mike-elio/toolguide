{
  "name": "adaptive-tool-guide-browser-tests",
  "private": true,
  "version": "0.1.0",
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:e2e": "node --test tests/e2e/*.cjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
